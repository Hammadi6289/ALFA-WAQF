import {
  require_react_dom
} from "./chunk-WF5B3A62.js";
import {
  require_react
} from "./chunk-E4M4WFON.js";
import {
  __commonJS,
  __toESM
} from "./chunk-7D4SUZUM.js";

// node_modules/react-is/cjs/react-is.development.js
var require_react_is_development = __commonJS({
  "node_modules/react-is/cjs/react-is.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var hasSymbol = typeof Symbol === "function" && Symbol.for;
        var REACT_ELEMENT_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.element") : 60103;
        var REACT_PORTAL_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.portal") : 60106;
        var REACT_FRAGMENT_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.fragment") : 60107;
        var REACT_STRICT_MODE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.strict_mode") : 60108;
        var REACT_PROFILER_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.profiler") : 60114;
        var REACT_PROVIDER_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.provider") : 60109;
        var REACT_CONTEXT_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.context") : 60110;
        var REACT_ASYNC_MODE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.async_mode") : 60111;
        var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.concurrent_mode") : 60111;
        var REACT_FORWARD_REF_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.forward_ref") : 60112;
        var REACT_SUSPENSE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.suspense") : 60113;
        var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.suspense_list") : 60120;
        var REACT_MEMO_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.memo") : 60115;
        var REACT_LAZY_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.lazy") : 60116;
        var REACT_BLOCK_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.block") : 60121;
        var REACT_FUNDAMENTAL_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.fundamental") : 60117;
        var REACT_RESPONDER_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.responder") : 60118;
        var REACT_SCOPE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.scope") : 60119;
        function isValidElementType(type) {
          return typeof type === "string" || typeof type === "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
          type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
        }
        function typeOf(object) {
          if (typeof object === "object" && object !== null) {
            var $$typeof = object.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                var type = object.type;
                switch (type) {
                  case REACT_ASYNC_MODE_TYPE:
                  case REACT_CONCURRENT_MODE_TYPE:
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                    return type;
                  default:
                    var $$typeofType = type && type.$$typeof;
                    switch ($$typeofType) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE:
                      case REACT_PROVIDER_TYPE:
                        return $$typeofType;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
          return void 0;
        }
        var AsyncMode = REACT_ASYNC_MODE_TYPE;
        var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
        var ContextConsumer = REACT_CONTEXT_TYPE;
        var ContextProvider = REACT_PROVIDER_TYPE;
        var Element = REACT_ELEMENT_TYPE;
        var ForwardRef = REACT_FORWARD_REF_TYPE;
        var Fragment6 = REACT_FRAGMENT_TYPE;
        var Lazy = REACT_LAZY_TYPE;
        var Memo = REACT_MEMO_TYPE;
        var Portal2 = REACT_PORTAL_TYPE;
        var Profiler = REACT_PROFILER_TYPE;
        var StrictMode = REACT_STRICT_MODE_TYPE;
        var Suspense = REACT_SUSPENSE_TYPE;
        var hasWarnedAboutDeprecatedIsAsyncMode = false;
        function isAsyncMode(object) {
          {
            if (!hasWarnedAboutDeprecatedIsAsyncMode) {
              hasWarnedAboutDeprecatedIsAsyncMode = true;
              console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
            }
          }
          return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
        }
        function isConcurrentMode(object) {
          return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
        }
        function isContextConsumer(object) {
          return typeOf(object) === REACT_CONTEXT_TYPE;
        }
        function isContextProvider(object) {
          return typeOf(object) === REACT_PROVIDER_TYPE;
        }
        function isElement(object) {
          return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
        }
        function isForwardRef(object) {
          return typeOf(object) === REACT_FORWARD_REF_TYPE;
        }
        function isFragment(object) {
          return typeOf(object) === REACT_FRAGMENT_TYPE;
        }
        function isLazy(object) {
          return typeOf(object) === REACT_LAZY_TYPE;
        }
        function isMemo(object) {
          return typeOf(object) === REACT_MEMO_TYPE;
        }
        function isPortal(object) {
          return typeOf(object) === REACT_PORTAL_TYPE;
        }
        function isProfiler(object) {
          return typeOf(object) === REACT_PROFILER_TYPE;
        }
        function isStrictMode(object) {
          return typeOf(object) === REACT_STRICT_MODE_TYPE;
        }
        function isSuspense(object) {
          return typeOf(object) === REACT_SUSPENSE_TYPE;
        }
        exports.AsyncMode = AsyncMode;
        exports.ConcurrentMode = ConcurrentMode;
        exports.ContextConsumer = ContextConsumer;
        exports.ContextProvider = ContextProvider;
        exports.Element = Element;
        exports.ForwardRef = ForwardRef;
        exports.Fragment = Fragment6;
        exports.Lazy = Lazy;
        exports.Memo = Memo;
        exports.Portal = Portal2;
        exports.Profiler = Profiler;
        exports.StrictMode = StrictMode;
        exports.Suspense = Suspense;
        exports.isAsyncMode = isAsyncMode;
        exports.isConcurrentMode = isConcurrentMode;
        exports.isContextConsumer = isContextConsumer;
        exports.isContextProvider = isContextProvider;
        exports.isElement = isElement;
        exports.isForwardRef = isForwardRef;
        exports.isFragment = isFragment;
        exports.isLazy = isLazy;
        exports.isMemo = isMemo;
        exports.isPortal = isPortal;
        exports.isProfiler = isProfiler;
        exports.isStrictMode = isStrictMode;
        exports.isSuspense = isSuspense;
        exports.isValidElementType = isValidElementType;
        exports.typeOf = typeOf;
      })();
    }
  }
});

// node_modules/react-is/index.js
var require_react_is = __commonJS({
  "node_modules/react-is/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_is_development();
    }
  }
});

// node_modules/object-assign/index.js
var require_object_assign = __commonJS({
  "node_modules/object-assign/index.js"(exports, module) {
    "use strict";
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var propIsEnumerable = Object.prototype.propertyIsEnumerable;
    function toObject(val) {
      if (val === null || val === void 0) {
        throw new TypeError("Object.assign cannot be called with null or undefined");
      }
      return Object(val);
    }
    function shouldUseNative() {
      try {
        if (!Object.assign) {
          return false;
        }
        var test1 = new String("abc");
        test1[5] = "de";
        if (Object.getOwnPropertyNames(test1)[0] === "5") {
          return false;
        }
        var test2 = {};
        for (var i = 0; i < 10; i++) {
          test2["_" + String.fromCharCode(i)] = i;
        }
        var order2 = Object.getOwnPropertyNames(test2).map(function(n) {
          return test2[n];
        });
        if (order2.join("") !== "0123456789") {
          return false;
        }
        var test3 = {};
        "abcdefghijklmnopqrst".split("").forEach(function(letter) {
          test3[letter] = letter;
        });
        if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") {
          return false;
        }
        return true;
      } catch (err) {
        return false;
      }
    }
    module.exports = shouldUseNative() ? Object.assign : function(target, source) {
      var from;
      var to = toObject(target);
      var symbols;
      for (var s = 1; s < arguments.length; s++) {
        from = Object(arguments[s]);
        for (var key in from) {
          if (hasOwnProperty.call(from, key)) {
            to[key] = from[key];
          }
        }
        if (getOwnPropertySymbols) {
          symbols = getOwnPropertySymbols(from);
          for (var i = 0; i < symbols.length; i++) {
            if (propIsEnumerable.call(from, symbols[i])) {
              to[symbols[i]] = from[symbols[i]];
            }
          }
        }
      }
      return to;
    };
  }
});

// node_modules/prop-types/lib/ReactPropTypesSecret.js
var require_ReactPropTypesSecret = __commonJS({
  "node_modules/prop-types/lib/ReactPropTypesSecret.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
    module.exports = ReactPropTypesSecret;
  }
});

// node_modules/prop-types/lib/has.js
var require_has = __commonJS({
  "node_modules/prop-types/lib/has.js"(exports, module) {
    module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
  }
});

// node_modules/prop-types/checkPropTypes.js
var require_checkPropTypes = __commonJS({
  "node_modules/prop-types/checkPropTypes.js"(exports, module) {
    "use strict";
    var printWarning = function() {
    };
    if (true) {
      ReactPropTypesSecret = require_ReactPropTypesSecret();
      loggedTypeFailures = {};
      has = require_has();
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x) {
        }
      };
    }
    var ReactPropTypesSecret;
    var loggedTypeFailures;
    var has;
    function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
      if (true) {
        for (var typeSpecName in typeSpecs) {
          if (has(typeSpecs, typeSpecName)) {
            var error;
            try {
              if (typeof typeSpecs[typeSpecName] !== "function") {
                var err = Error(
                  (componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
                );
                err.name = "Invariant Violation";
                throw err;
              }
              error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
            } catch (ex) {
              error = ex;
            }
            if (error && !(error instanceof Error)) {
              printWarning(
                (componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
              );
            }
            if (error instanceof Error && !(error.message in loggedTypeFailures)) {
              loggedTypeFailures[error.message] = true;
              var stack = getStack ? getStack() : "";
              printWarning(
                "Failed " + location + " type: " + error.message + (stack != null ? stack : "")
              );
            }
          }
        }
      }
    }
    checkPropTypes.resetWarningCache = function() {
      if (true) {
        loggedTypeFailures = {};
      }
    };
    module.exports = checkPropTypes;
  }
});

// node_modules/prop-types/factoryWithTypeCheckers.js
var require_factoryWithTypeCheckers = __commonJS({
  "node_modules/prop-types/factoryWithTypeCheckers.js"(exports, module) {
    "use strict";
    var ReactIs = require_react_is();
    var assign = require_object_assign();
    var ReactPropTypesSecret = require_ReactPropTypesSecret();
    var has = require_has();
    var checkPropTypes = require_checkPropTypes();
    var printWarning = function() {
    };
    if (true) {
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x) {
        }
      };
    }
    function emptyFunctionThatReturnsNull() {
      return null;
    }
    module.exports = function(isValidElement, throwOnDirectAccess) {
      var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
      var FAUX_ITERATOR_SYMBOL = "@@iterator";
      function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === "function") {
          return iteratorFn;
        }
      }
      var ANONYMOUS = "<<anonymous>>";
      var ReactPropTypes = {
        array: createPrimitiveTypeChecker("array"),
        bigint: createPrimitiveTypeChecker("bigint"),
        bool: createPrimitiveTypeChecker("boolean"),
        func: createPrimitiveTypeChecker("function"),
        number: createPrimitiveTypeChecker("number"),
        object: createPrimitiveTypeChecker("object"),
        string: createPrimitiveTypeChecker("string"),
        symbol: createPrimitiveTypeChecker("symbol"),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
      };
      function is(x, y) {
        if (x === y) {
          return x !== 0 || 1 / x === 1 / y;
        } else {
          return x !== x && y !== y;
        }
      }
      function PropTypeError(message, data) {
        this.message = message;
        this.data = data && typeof data === "object" ? data : {};
        this.stack = "";
      }
      PropTypeError.prototype = Error.prototype;
      function createChainableTypeChecker(validate) {
        if (true) {
          var manualPropTypeCallCache = {};
          var manualPropTypeWarningCount = 0;
        }
        function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
          componentName = componentName || ANONYMOUS;
          propFullName = propFullName || propName;
          if (secret !== ReactPropTypesSecret) {
            if (throwOnDirectAccess) {
              var err = new Error(
                "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
              );
              err.name = "Invariant Violation";
              throw err;
            } else if (typeof console !== "undefined") {
              var cacheKey = componentName + ":" + propName;
              if (!manualPropTypeCallCache[cacheKey] && // Avoid spamming the console because they are often not actionable except for lib authors
              manualPropTypeWarningCount < 3) {
                printWarning(
                  "You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
                );
                manualPropTypeCallCache[cacheKey] = true;
                manualPropTypeWarningCount++;
              }
            }
          }
          if (props[propName] == null) {
            if (isRequired) {
              if (props[propName] === null) {
                return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
              }
              return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
            }
            return null;
          } else {
            return validate(props, propName, componentName, location, propFullName);
          }
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
      }
      function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location, propFullName, secret) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== expectedType) {
            var preciseType = getPreciseType(propValue);
            return new PropTypeError(
              "Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."),
              { expectedType }
            );
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createAnyTypeChecker() {
        return createChainableTypeChecker(emptyFunctionThatReturnsNull);
      }
      function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
          }
          var propValue = props[propName];
          if (!Array.isArray(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
          }
          for (var i = 0; i < propValue.length; i++) {
            var error = typeChecker(propValue, i, componentName, location, propFullName + "[" + i + "]", ReactPropTypesSecret);
            if (error instanceof Error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!isValidElement(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!ReactIs.isValidElementType(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location, propFullName) {
          if (!(props[propName] instanceof expectedClass)) {
            var expectedClassName = expectedClass.name || ANONYMOUS;
            var actualClassName = getClassName(props[propName]);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
          if (true) {
            if (arguments.length > 1) {
              printWarning(
                "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
              );
            } else {
              printWarning("Invalid argument supplied to oneOf, expected an array.");
            }
          }
          return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          for (var i = 0; i < expectedValues.length; i++) {
            if (is(propValue, expectedValues[i])) {
              return null;
            }
          }
          var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
            var type = getPreciseType(value);
            if (type === "symbol") {
              return String(value);
            }
            return value;
          });
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
          }
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
          }
          for (var key in propValue) {
            if (has(propValue, key)) {
              var error = typeChecker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
              if (error instanceof Error) {
                return error;
              }
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
          true ? printWarning("Invalid argument supplied to oneOfType, expected an instance of array.") : void 0;
          return emptyFunctionThatReturnsNull;
        }
        for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
          var checker = arrayOfTypeCheckers[i];
          if (typeof checker !== "function") {
            printWarning(
              "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i + "."
            );
            return emptyFunctionThatReturnsNull;
          }
        }
        function validate(props, propName, componentName, location, propFullName) {
          var expectedTypes = [];
          for (var i2 = 0; i2 < arrayOfTypeCheckers.length; i2++) {
            var checker2 = arrayOfTypeCheckers[i2];
            var checkerResult = checker2(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
            if (checkerResult == null) {
              return null;
            }
            if (checkerResult.data && has(checkerResult.data, "expectedType")) {
              expectedTypes.push(checkerResult.data.expectedType);
            }
          }
          var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createNodeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          if (!isNode(props[propName])) {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function invalidValidatorError(componentName, location, propFullName, key, type) {
        return new PropTypeError(
          (componentName || "React class") + ": " + location + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`."
        );
      }
      function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          for (var key in shapeTypes) {
            var checker = shapeTypes[key];
            if (typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
            }
            var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          var allKeys = assign({}, props[propName], shapeTypes);
          for (var key in allKeys) {
            var checker = shapeTypes[key];
            if (has(shapeTypes, key) && typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
            }
            if (!checker) {
              return new PropTypeError(
                "Invalid " + location + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  ")
              );
            }
            var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function isNode(propValue) {
        switch (typeof propValue) {
          case "number":
          case "string":
          case "undefined":
            return true;
          case "boolean":
            return !propValue;
          case "object":
            if (Array.isArray(propValue)) {
              return propValue.every(isNode);
            }
            if (propValue === null || isValidElement(propValue)) {
              return true;
            }
            var iteratorFn = getIteratorFn(propValue);
            if (iteratorFn) {
              var iterator = iteratorFn.call(propValue);
              var step;
              if (iteratorFn !== propValue.entries) {
                while (!(step = iterator.next()).done) {
                  if (!isNode(step.value)) {
                    return false;
                  }
                }
              } else {
                while (!(step = iterator.next()).done) {
                  var entry = step.value;
                  if (entry) {
                    if (!isNode(entry[1])) {
                      return false;
                    }
                  }
                }
              }
            } else {
              return false;
            }
            return true;
          default:
            return false;
        }
      }
      function isSymbol(propType, propValue) {
        if (propType === "symbol") {
          return true;
        }
        if (!propValue) {
          return false;
        }
        if (propValue["@@toStringTag"] === "Symbol") {
          return true;
        }
        if (typeof Symbol === "function" && propValue instanceof Symbol) {
          return true;
        }
        return false;
      }
      function getPropType(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) {
          return "array";
        }
        if (propValue instanceof RegExp) {
          return "object";
        }
        if (isSymbol(propType, propValue)) {
          return "symbol";
        }
        return propType;
      }
      function getPreciseType(propValue) {
        if (typeof propValue === "undefined" || propValue === null) {
          return "" + propValue;
        }
        var propType = getPropType(propValue);
        if (propType === "object") {
          if (propValue instanceof Date) {
            return "date";
          } else if (propValue instanceof RegExp) {
            return "regexp";
          }
        }
        return propType;
      }
      function getPostfixForTypeWarning(value) {
        var type = getPreciseType(value);
        switch (type) {
          case "array":
          case "object":
            return "an " + type;
          case "boolean":
          case "date":
          case "regexp":
            return "a " + type;
          default:
            return type;
        }
      }
      function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) {
          return ANONYMOUS;
        }
        return propValue.constructor.name;
      }
      ReactPropTypes.checkPropTypes = checkPropTypes;
      ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
      ReactPropTypes.PropTypes = ReactPropTypes;
      return ReactPropTypes;
    };
  }
});

// node_modules/prop-types/index.js
var require_prop_types = __commonJS({
  "node_modules/prop-types/index.js"(exports, module) {
    if (true) {
      ReactIs = require_react_is();
      throwOnDirectAccess = true;
      module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, throwOnDirectAccess);
    } else {
      module.exports = null();
    }
    var ReactIs;
    var throwOnDirectAccess;
  }
});

// node_modules/yet-another-react-lightbox/dist/types.js
var import_prop_types = __toESM(require_prop_types(), 1);
var SlideTypesPropTypes = [
  import_prop_types.default.shape({
    src: import_prop_types.default.string.isRequired,
    title: import_prop_types.default.string,
    aspectRatio: import_prop_types.default.number,
    srcSet: import_prop_types.default.oneOfType([
      import_prop_types.default.string.isRequired,
      import_prop_types.default.arrayOf(import_prop_types.default.shape({
        src: import_prop_types.default.string.isRequired,
        width: import_prop_types.default.number.isRequired
      }).isRequired).isRequired
    ])
  })
];
var LightboxPropTypes = {
  open: import_prop_types.default.bool.isRequired,
  close: import_prop_types.default.func.isRequired,
  index: import_prop_types.default.number.isRequired,
  slides: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType(SlideTypesPropTypes).isRequired).isRequired,
  render: import_prop_types.default.shape({}).isRequired,
  plugins: import_prop_types.default.arrayOf(import_prop_types.default.func.isRequired).isRequired,
  toolbar: import_prop_types.default.shape({
    buttons: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["close"]), import_prop_types.default.node])).isRequired
  }).isRequired,
  labels: import_prop_types.default.shape({}).isRequired,
  carousel: import_prop_types.default.shape({
    finite: import_prop_types.default.bool.isRequired,
    preload: import_prop_types.default.number.isRequired,
    padding: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
    spacing: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired
  }).isRequired,
  animation: import_prop_types.default.shape({
    fade: import_prop_types.default.number.isRequired,
    swipe: import_prop_types.default.number.isRequired
  }).isRequired,
  on: import_prop_types.default.shape({}).isRequired
};
var LightboxDefaultProps = {
  open: false,
  close: () => {
  },
  index: 0,
  slides: [],
  render: {},
  plugins: [],
  toolbar: { buttons: ["close"] },
  labels: {},
  animation: {
    fade: 330,
    swipe: 500
  },
  carousel: {
    finite: false,
    preload: 2,
    padding: "16px",
    spacing: "30%"
  },
  on: {}
};

// node_modules/yet-another-react-lightbox/dist/Lightbox.js
var React18 = __toESM(require_react(), 1);

// node_modules/yet-another-react-lightbox/dist/core/utils.js
var React = __toESM(require_react(), 1);
var clsx = (...classes) => [...classes].filter((cls) => Boolean(cls)).join(" ");
var cssPrefix = "yarl__";
var cssClass = (name) => `${cssPrefix}${name}`;
var cssVar = (name) => `--${cssPrefix}${name}`;
var label = (labels, lbl) => labels && labels[lbl] ? labels[lbl] : lbl;
var cleanup = (...cleaners) => () => {
  cleaners.forEach((cleaner) => {
    cleaner();
  });
};
var makeUseContext = (name, contextName, context) => () => {
  const ctx = React.useContext(context);
  if (!ctx) {
    throw new Error(`${name} must be used within a ${contextName}.Provider`);
  }
  return ctx;
};

// node_modules/yet-another-react-lightbox/dist/core/config.js
var createModule = (name, component) => ({
  name,
  component
});
var createNode = (module, children) => ({
  module,
  children
});
var traverseNode = (node, target, apply) => {
  if (node.module.name === target) {
    return apply(node);
  }
  if (node.children) {
    return [
      createNode(node.module, node.children.flatMap((n) => {
        var _a;
        return (_a = traverseNode(n, target, apply)) !== null && _a !== void 0 ? _a : [];
      }))
    ];
  }
  return [node];
};
var traverse = (nodes, target, apply) => nodes.flatMap((node) => {
  var _a;
  return (_a = traverseNode(node, target, apply)) !== null && _a !== void 0 ? _a : [];
});
var withPlugins = (root, plugins) => {
  let config = root;
  const augmentations = [];
  const addParent = (target, module) => {
    if (target === "") {
      config = [createNode(module, config)];
      return;
    }
    config = traverse(config, target, (node) => [createNode(module, [node])]);
  };
  const addChild = (target, module, precede) => {
    config = traverse(config, target, (node) => {
      var _a;
      return [
        createNode(node.module, [
          ...precede ? [createNode(module)] : [],
          ...(_a = node.children) !== null && _a !== void 0 ? _a : [],
          ...!precede ? [createNode(module)] : []
        ])
      ];
    });
  };
  const addSibling = (target, module, precede) => {
    config = traverse(config, target, (node) => [
      ...precede ? [createNode(module)] : [],
      node,
      ...!precede ? [createNode(module)] : []
    ]);
  };
  const replace = (target, module) => {
    config = traverse(config, target, (node) => [createNode(module, node.children)]);
  };
  const remove = (target) => {
    config = traverse(config, target, (node) => node.children);
  };
  const augment = (augmentation) => {
    augmentations.push(augmentation);
  };
  plugins === null || plugins === void 0 ? void 0 : plugins.forEach((plugin) => {
    plugin({
      addParent,
      addChild,
      addSibling,
      replace,
      remove,
      augment
    });
  });
  return {
    config,
    augmentation: (props) => augmentations.reduce((acc, augmentation) => augmentation(acc), props)
  };
};

// node_modules/yet-another-react-lightbox/dist/core/hooks/useContainerRect.js
var React2 = __toESM(require_react(), 1);
var useContainerRect = () => {
  const [containerRect, setContainerRect] = React2.useState();
  const containerRef = React2.useRef(null);
  const observerRef = React2.useRef();
  const setContainerRef = React2.useCallback((node) => {
    containerRef.current = node;
    if (observerRef.current) {
      observerRef.current.disconnect();
      observerRef.current = void 0;
    }
    const updateContainerRect = () => {
      const width = node === null || node === void 0 ? void 0 : node.clientWidth;
      const height = node === null || node === void 0 ? void 0 : node.clientHeight;
      setContainerRect(width !== void 0 && height !== void 0 ? {
        width,
        height
      } : void 0);
    };
    updateContainerRect();
    if (node && typeof ResizeObserver !== "undefined") {
      observerRef.current = new ResizeObserver(updateContainerRect);
      observerRef.current.observe(node);
    }
  }, []);
  return React2.useMemo(() => ({
    setContainerRef,
    containerRef,
    containerRect
  }), [setContainerRef, containerRef, containerRect]);
};

// node_modules/yet-another-react-lightbox/dist/core/hooks/useEnhancedEffect.js
var React3 = __toESM(require_react(), 1);
var useEnhancedEffect = typeof window !== "undefined" ? React3.useLayoutEffect : React3.useEffect;

// node_modules/yet-another-react-lightbox/dist/core/hooks/useLatest.js
var React4 = __toESM(require_react(), 1);
var useLatest = (value) => {
  const ref = React4.useRef(value);
  ref.current = value;
  return ref;
};

// node_modules/yet-another-react-lightbox/dist/core/hooks/useSensors.js
var React5 = __toESM(require_react(), 1);
var useSensors = () => {
  const [subscribers] = React5.useState({});
  return React5.useMemo(() => {
    const notifySubscribers = (type, event) => {
      var _a;
      (_a = subscribers[type]) === null || _a === void 0 ? void 0 : _a.forEach((listener) => listener(event));
    };
    return {
      registerSensors: {
        onPointerDown: (event) => notifySubscribers("onPointerDown", event),
        onPointerMove: (event) => notifySubscribers("onPointerMove", event),
        onPointerUp: (event) => notifySubscribers("onPointerUp", event),
        onPointerLeave: (event) => notifySubscribers("onPointerLeave", event),
        onPointerCancel: (event) => notifySubscribers("onPointerCancel", event),
        onTouchStart: (event) => notifySubscribers("onTouchStart", event),
        onTouchMove: (event) => notifySubscribers("onTouchMove", event),
        onTouchEnd: (event) => notifySubscribers("onTouchEnd", event),
        onTouchCancel: (event) => notifySubscribers("onTouchCancel", event),
        onKeyDown: (event) => notifySubscribers("onKeyDown", event),
        onKeyUp: (event) => notifySubscribers("onKeyUp", event),
        onWheel: (event) => notifySubscribers("onWheel", event)
      },
      subscribeSensors: (type, callback) => {
        var _a;
        if (!subscribers[type]) {
          subscribers[type] = [];
        }
        (_a = subscribers[type]) === null || _a === void 0 ? void 0 : _a.push(callback);
        return () => {
          const listeners = subscribers[type];
          if (listeners) {
            listeners.splice(0, listeners.length, ...listeners.filter((el) => el !== callback));
          }
        };
      }
    };
  }, [subscribers]);
};

// node_modules/yet-another-react-lightbox/dist/core/contexts/Events.js
var React6 = __toESM(require_react(), 1);
var EventsContext = React6.createContext(null);
var useEvents = makeUseContext("useEvents", "EventsContext", EventsContext);
var EventsProvider = ({ children }) => {
  const subscriptions = React6.useRef({});
  const unsubscribe = (topic, callback) => {
    if (subscriptions.current[topic]) {
      subscriptions.current[topic] = subscriptions.current[topic].filter((cb) => cb !== callback);
    }
  };
  const subscribe = (topic, callback) => {
    if (!subscriptions.current[topic]) {
      subscriptions.current[topic] = [];
    }
    subscriptions.current[topic].push(callback);
    return () => unsubscribe(topic, callback);
  };
  const publish = (topic, event) => {
    var _a;
    (_a = subscriptions.current[topic]) === null || _a === void 0 ? void 0 : _a.forEach((callback) => callback(topic, event));
  };
  React6.useEffect(() => () => {
    subscriptions.current = {};
  }, []);
  const context = React6.useRef({
    subscribe,
    unsubscribe,
    publish
  });
  return React6.createElement(EventsContext.Provider, { value: context.current }, children);
};

// node_modules/yet-another-react-lightbox/dist/core/contexts/Timeouts.js
var React7 = __toESM(require_react(), 1);
var TimeoutsContext = React7.createContext(null);
var useTimeouts = makeUseContext("useTimeouts", "TimeoutsContext", TimeoutsContext);
var TimeoutsProvider = ({ children }) => {
  const timeouts = React7.useRef([]);
  const removeTimeout = (id) => {
    timeouts.current.splice(0, timeouts.current.length, ...timeouts.current.filter((tid) => tid !== id));
  };
  const setTimeout = (func, time) => {
    const id = window.setTimeout(() => {
      removeTimeout(id);
      func();
    }, time);
    timeouts.current.push(id);
    return id;
  };
  const clearTimeout = (id) => {
    if (typeof id !== "undefined") {
      removeTimeout(id);
      window.clearTimeout(id);
    }
  };
  const clearTimeouts = () => {
    timeouts.current.forEach((tid) => window.clearTimeout(tid));
    timeouts.current.splice(0, timeouts.current.length);
  };
  React7.useEffect(() => () => clearTimeouts(), []);
  const context = React7.useRef({
    setTimeout,
    clearTimeout,
    clearTimeouts
  });
  return React7.createElement(TimeoutsContext.Provider, { value: context.current }, children);
};

// node_modules/yet-another-react-lightbox/dist/core/components/IconButton.js
var React8 = __toESM(require_react(), 1);
var IconButton = ({ label: label2, className, icon: Icon, renderIcon, onClick, ...rest }) => React8.createElement("button", { type: "button", "aria-label": label2, className: clsx(cssClass("button"), className), onClick, ...rest }, renderIcon ? renderIcon() : React8.createElement(Icon, { className: cssClass("icon") }));

// node_modules/yet-another-react-lightbox/dist/core/components/Icons.js
var React9 = __toESM(require_react(), 1);
var createIcon = (name, glyph) => {
  const icon = (props) => React9.createElement(
    "svg",
    { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 24 24", width: "24", height: "24", "aria-hidden": "true", focusable: "false", ...props },
    React9.createElement(
      "g",
      { fill: "currentColor" },
      React9.createElement("path", { d: "M0 0h24v24H0z", fill: "none" }),
      glyph
    )
  );
  icon.displayName = name;
  return icon;
};
var CloseIcon = createIcon("Close", React9.createElement("path", { d: "M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" }));
var PreviousIcon = createIcon("Previous", React9.createElement("path", { d: "M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z" }));
var NextIcon = createIcon("Next", React9.createElement("path", { d: "M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z" }));
var LoadingIcon = createIcon("Loading", React9.createElement(React9.Fragment, null, Array.from({ length: 8 }).map((_, index, array) => React9.createElement("line", { key: index, x1: "12", y1: "6.5", x2: "12", y2: "1.8", strokeLinecap: "round", strokeWidth: "2.6", stroke: "currentColor", strokeOpacity: 1 / array.length * (index + 1), transform: `rotate(${360 / array.length * index}, 12, 12)` }))));
var ErrorIcon = createIcon("Error", React9.createElement("path", { d: "M21.9,21.9l-8.49-8.49l0,0L3.59,3.59l0,0L2.1,2.1L0.69,3.51L3,5.83V19c0,1.1,0.9,2,2,2h13.17l2.31,2.31L21.9,21.9z M5,18 l3.5-4.5l2.5,3.01L12.17,15l3,3H5z M21,18.17L5.83,3H19c1.1,0,2,0.9,2,2V18.17z" }));

// node_modules/yet-another-react-lightbox/dist/core/components/ImageSlide.js
var React11 = __toESM(require_react(), 1);

// node_modules/yet-another-react-lightbox/dist/core/modules/Controller.js
var React10 = __toESM(require_react(), 1);
var SWIPE_OFFSET_THRESHOLD = 30;
var ControllerContext = React10.createContext(null);
var useController = makeUseContext("useController", "ControllerContext", ControllerContext);
var Controller = ({ children, ...props }) => {
  const { containerRef, setContainerRef, containerRect } = useContainerRect();
  const { registerSensors, subscribeSensors } = useSensors();
  const { subscribe, publish } = useEvents();
  const { setTimeout, clearTimeout } = useTimeouts();
  const [state, setState] = React10.useState({
    currentIndex: props.index,
    globalIndex: props.index
  });
  const refs = React10.useRef({
    state,
    props,
    swipeOffset: 0,
    swipeIntent: 0,
    swipeAnimationDuration: props.animation.swipe,
    wheelResidualMomentum: 0,
    pointers: []
  });
  refs.current.state = state;
  refs.current.props = props;
  refs.current.containerRect = containerRect;
  useEnhancedEffect(() => {
    const preventDefault = (event) => event.preventDefault();
    const node = containerRef.current;
    if (node) {
      node.addEventListener("wheel", preventDefault, { passive: false });
    }
    return () => {
      if (node) {
        node.removeEventListener("wheel", preventDefault);
      }
    };
  }, [containerRef]);
  React10.useEffect(() => {
    var _a;
    (_a = containerRef.current) === null || _a === void 0 ? void 0 : _a.focus();
  }, [containerRef]);
  React10.useEffect(() => {
    var _a, _b;
    (_b = (_a = refs.current.props.on).view) === null || _b === void 0 ? void 0 : _b.call(_a, state.currentIndex);
  }, [state.currentIndex]);
  const updateSwipeOffset = React10.useCallback(() => {
    var _a, _b;
    const offsetVar = cssVar("swipe_offset");
    if (refs.current.swipeOffset !== 0) {
      (_a = containerRef.current) === null || _a === void 0 ? void 0 : _a.style.setProperty(offsetVar, `${Math.round(refs.current.swipeOffset)}px`);
    } else {
      (_b = containerRef.current) === null || _b === void 0 ? void 0 : _b.style.removeProperty(offsetVar);
    }
  }, [containerRef]);
  useEnhancedEffect(() => {
    updateSwipeOffset();
  });
  const rerender = React10.useCallback(() => {
    setState((prev) => ({ ...prev }));
  }, []);
  const resetSwipe = React10.useCallback(() => {
    const { current } = refs;
    current.swipeOffset = 0;
    current.swipeIntent = 0;
    current.swipeStartTime = void 0;
    clearTimeout(current.swipeResetCleanup);
    current.swipeResetCleanup = void 0;
    clearTimeout(current.swipeIntentCleanup);
    current.swipeIntentCleanup = void 0;
  }, [clearTimeout]);
  const isSwipeValid = React10.useCallback((offset) => {
    const { state: { currentIndex }, props: { carousel, slides } } = refs.current;
    return !(carousel.finite && (offset > 0 && currentIndex === 0 || offset < 0 && currentIndex === slides.length - 1));
  }, []);
  const swipe = React10.useCallback((direction) => {
    var _a;
    const { current } = refs;
    const slidesCount = current.props.slides.length;
    const swipeAnimationDuration = current.props.animation.swipe;
    const { currentIndex, globalIndex } = current.state;
    const { swipeOffset } = current;
    let newSwipeState = "swipe-animation";
    let newSwipeAnimationDuration = swipeAnimationDuration;
    if (!direction) {
      const containerWidth = (_a = current.containerRect) === null || _a === void 0 ? void 0 : _a.width;
      const elapsedTime = current.swipeStartTime ? Date.now() - current.swipeStartTime : 0;
      const expectedTime = containerWidth ? swipeAnimationDuration / containerWidth * Math.abs(swipeOffset) : swipeAnimationDuration;
      if (containerWidth && (swipeOffset !== 0 && elapsedTime < swipeAnimationDuration || Math.abs(swipeOffset) > 0.5 * containerWidth)) {
        newSwipeAnimationDuration = swipeAnimationDuration / containerWidth * (containerWidth - Math.abs(swipeOffset));
        if (elapsedTime < expectedTime) {
          newSwipeAnimationDuration = newSwipeAnimationDuration / expectedTime * Math.max(elapsedTime, expectedTime / 5);
        }
        direction = swipeOffset > 0 ? "prev" : "next";
      } else {
        newSwipeAnimationDuration = swipeAnimationDuration / 2;
      }
    }
    const newState = {};
    if (direction === "prev") {
      if (isSwipeValid(swipeOffset)) {
        newState.currentIndex = (currentIndex - 1 + slidesCount) % slidesCount;
        newState.globalIndex = globalIndex - 1;
      } else {
        newSwipeState = void 0;
        newSwipeAnimationDuration = swipeAnimationDuration;
      }
    } else if (direction === "next") {
      if (isSwipeValid(swipeOffset)) {
        newState.currentIndex = (currentIndex + 1) % slidesCount;
        newState.globalIndex = globalIndex + 1;
      } else {
        newSwipeState = void 0;
        newSwipeAnimationDuration = swipeAnimationDuration;
      }
    }
    resetSwipe();
    current.swipeState = newSwipeState;
    current.swipeAnimationDuration = newSwipeAnimationDuration;
    if (newSwipeState) {
      setTimeout(() => {
        current.swipeState = void 0;
        current.swipeAnimationDuration = current.props.animation.swipe;
        rerender();
      }, newSwipeAnimationDuration);
    }
    setState((prev) => ({ ...prev, ...newState }));
  }, [setTimeout, resetSwipe, isSwipeValid, rerender]);
  React10.useEffect(() => cleanup(subscribe("prev", () => swipe("prev")), subscribe("next", () => swipe("next"))), [subscribe, swipe]);
  React10.useEffect(() => subscribeSensors("onKeyUp", (event) => {
    if (event.code === "Escape") {
      publish("close");
    }
  }), [subscribeSensors, publish]);
  const clearPointer = React10.useCallback((event) => {
    const { current } = refs;
    if (current.activePointer === event.pointerId) {
      current.activePointer = void 0;
    }
    current.pointers.splice(0, current.pointers.length, ...current.pointers.filter((p) => p.pointerId !== event.pointerId));
  }, []);
  const addPointer = React10.useCallback((event) => {
    clearPointer(event);
    refs.current.pointers.push(event);
  }, [clearPointer]);
  const onPointerDown = React10.useCallback((event) => {
    addPointer(event);
  }, [addPointer]);
  const onPointerMove = React10.useCallback((event) => {
    const { current } = refs;
    const original = current.pointers.find((p) => p.pointerId === event.pointerId);
    if (original) {
      const deltaX = event.clientX - original.clientX;
      const deltaY = event.clientY - original.clientY;
      if (!current.swipeState) {
        if (isSwipeValid(deltaX) && Math.abs(deltaX) > Math.abs(deltaY) && Math.abs(deltaX) > SWIPE_OFFSET_THRESHOLD) {
          addPointer(event);
          current.activePointer = event.pointerId;
          current.swipeStartTime = Date.now();
          current.swipeState = "swipe";
          rerender();
        }
      } else if (current.swipeState === "swipe") {
        if (event.pointerId === current.activePointer) {
          current.swipeOffset = deltaX;
          updateSwipeOffset();
        }
      }
    }
  }, [addPointer, updateSwipeOffset, isSwipeValid, rerender]);
  const onPointerUp = React10.useCallback((event) => {
    const { current } = refs;
    if (current.pointers.find((p) => p.pointerId === event.pointerId) && current.swipeState === "swipe" && current.activePointer === event.pointerId) {
      swipe();
    }
    clearPointer(event);
  }, [clearPointer, swipe]);
  React10.useEffect(() => cleanup(subscribeSensors("onPointerDown", onPointerDown), subscribeSensors("onPointerMove", onPointerMove), subscribeSensors("onPointerUp", onPointerUp), subscribeSensors("onPointerLeave", onPointerUp), subscribeSensors("onPointerCancel", onPointerUp)), [subscribeSensors, onPointerDown, onPointerMove, onPointerUp]);
  const onWheel = React10.useCallback((event) => {
    var _a;
    if (event.ctrlKey) {
      return;
    }
    if (Math.abs(event.deltaY) > Math.abs(event.deltaX)) {
      return;
    }
    const { current } = refs;
    if (!current.swipeState) {
      if (Math.abs(event.deltaX) <= 1.2 * Math.abs(current.wheelResidualMomentum)) {
        current.wheelResidualMomentum = event.deltaX;
        return;
      }
      current.swipeIntent += event.deltaX;
      clearTimeout(current.swipeIntentCleanup);
      if (Math.abs(current.swipeIntent) > SWIPE_OFFSET_THRESHOLD) {
        current.swipeStartTime = Date.now();
        current.swipeIntent = 0;
        current.wheelResidualMomentum = 0;
        current.swipeState = "swipe";
        rerender();
      } else {
        current.swipeIntentCleanup = setTimeout(() => {
          current.swipeIntent = 0;
          current.swipeIntentCleanup = void 0;
        }, current.props.animation.swipe);
      }
    } else if (current.swipeState === "swipe") {
      const containerWidth = (_a = current.containerRect) === null || _a === void 0 ? void 0 : _a.width;
      if (containerWidth) {
        current.swipeOffset -= event.deltaX;
        current.swipeOffset = Math.min(Math.abs(current.swipeOffset), containerWidth) * Math.sign(current.swipeOffset);
        updateSwipeOffset();
        clearTimeout(current.swipeResetCleanup);
        if (Math.abs(current.swipeOffset) > 0.2 * containerWidth) {
          current.wheelResidualMomentum = event.deltaX;
          swipe();
          return;
        }
        const currentSwipeOffset = current.swipeOffset;
        current.swipeResetCleanup = setTimeout(() => {
          current.swipeResetCleanup = void 0;
          if (current.swipeState === "swipe" && current.swipeOffset === currentSwipeOffset) {
            resetSwipe();
            current.swipeState = void 0;
            rerender();
          }
        }, 2 * current.props.animation.swipe);
      }
    } else {
      current.wheelResidualMomentum = event.deltaX;
    }
  }, [updateSwipeOffset, setTimeout, clearTimeout, swipe, resetSwipe, rerender]);
  React10.useEffect(() => subscribeSensors("onWheel", onWheel), [subscribeSensors, onWheel]);
  const context = React10.useMemo(() => ({
    containerRef,
    containerRect,
    currentIndex: state.currentIndex,
    globalIndex: state.globalIndex,
    subscribeSensors
  }), [containerRef, containerRect, state.currentIndex, state.globalIndex, subscribeSensors]);
  return React10.createElement("div", { ref: setContainerRef, className: clsx(cssClass("container"), refs.current.swipeState === "swipe" && cssClass("container_swipe")), style: refs.current.swipeAnimationDuration !== LightboxDefaultProps.animation.swipe ? {
    [cssVar("swipe_animation_duration")]: `${Math.round(refs.current.swipeAnimationDuration)}ms`
  } : void 0, role: "presentation", "aria-live": "polite", tabIndex: -1, ...registerSensors }, containerRect && React10.createElement(ControllerContext.Provider, { value: context }, children));
};
var ControllerModule = createModule("controller", Controller);

// node_modules/yet-another-react-lightbox/dist/core/components/ImageSlide.js
var ImageSlide = ({ slide: image, render }) => {
  var _a, _b, _c;
  const [state, setState] = React11.useState("loading");
  const latestState = useLatest(state);
  const imageRef = React11.useRef(null);
  const { containerRect } = useController();
  const handleLoading = React11.useCallback((img) => {
    if (latestState.current === "complete") {
      return;
    }
    ("decode" in img ? img.decode() : Promise.resolve()).catch(() => {
    }).then(() => {
      if (!img.parentNode) {
        return;
      }
      setState("complete");
    });
  }, [latestState]);
  const setImageRef = React11.useCallback((img) => {
    imageRef.current = img;
    if (img === null || img === void 0 ? void 0 : img.complete) {
      handleLoading(img);
    }
  }, [handleLoading]);
  const onLoad = React11.useCallback((event) => {
    handleLoading(event.currentTarget);
  }, [handleLoading]);
  const onError = React11.useCallback(() => {
    setState("error");
  }, []);
  return React11.createElement(
    React11.Fragment,
    null,
    React11.createElement("img", { ref: setImageRef, onLoad, onError, className: clsx(cssClass("slide_image"), state !== "complete" && cssClass("slide_image_loading")), draggable: false, alt: image.title, ...image.srcSet ? {
      sizes: `${Math.ceil(Math.min(image.aspectRatio ? containerRect.height * image.aspectRatio : Number.MAX_VALUE, containerRect.width) / window.innerWidth * 100)}vw`,
      srcSet: image.srcSet.sort((a, b) => a.width - b.width).map((item) => `${item.src} ${item.width}w`).join(", "),
      style: {
        maxWidth: `${Math.max(...image.srcSet.map((x) => x.width))}px`
      }
    } : {
      style: ((_b = (_a = imageRef.current) === null || _a === void 0 ? void 0 : _a.naturalWidth) !== null && _b !== void 0 ? _b : 0) > 0 ? {
        maxWidth: `${(_c = imageRef.current) === null || _c === void 0 ? void 0 : _c.naturalWidth}px`
      } : void 0
    }, src: image.src }),
    state !== "complete" && React11.createElement(
      "div",
      { className: cssClass("slide_placeholder") },
      state === "loading" && (render.iconLoading ? render.iconLoading() : React11.createElement(LoadingIcon, { className: clsx(cssClass("icon"), cssClass("slide_loading")) })),
      state === "error" && (render.iconError ? render.iconError() : React11.createElement(ErrorIcon, { className: clsx(cssClass("icon"), cssClass("slide_error")) }))
    )
  );
};

// node_modules/yet-another-react-lightbox/dist/core/modules/Carousel.js
var React12 = __toESM(require_react(), 1);
var CarouselSlide = ({ slide, offset, render }) => {
  var _a;
  return React12.createElement("div", { className: clsx(cssClass("slide"), cssClass("flex_center")), style: { [cssVar("slide_offset")]: offset } }, ((_a = render.slide) === null || _a === void 0 ? void 0 : _a.call(render, slide)) || "src" in slide && React12.createElement(ImageSlide, { slide, render }));
};
var Carousel = (props) => {
  const { slides, carousel: { finite, preload, padding, spacing }, render } = props;
  const { currentIndex, globalIndex } = useController();
  const items = [];
  if ((slides === null || slides === void 0 ? void 0 : slides.length) > 0) {
    for (let i = currentIndex - preload; i < currentIndex; i += 1) {
      if (!finite || i >= 0) {
        items.push(React12.createElement(CarouselSlide, { key: globalIndex + i - currentIndex, slide: slides[(i + preload * slides.length) % slides.length], offset: i - currentIndex, render }));
      }
    }
    items.push(React12.createElement(CarouselSlide, { key: globalIndex, slide: slides[currentIndex], offset: 0, render }));
    for (let i = currentIndex + 1; i <= currentIndex + preload; i += 1) {
      if (!finite || i <= slides.length - 1) {
        items.push(React12.createElement(CarouselSlide, { key: globalIndex + i - currentIndex, slide: slides[i % slides.length], offset: i - currentIndex, render }));
      }
    }
  }
  return React12.createElement("div", { className: cssClass("carousel"), style: {
    ...padding !== LightboxDefaultProps.carousel.padding ? { [cssVar("carousel_padding")]: padding } : null,
    ...spacing !== LightboxDefaultProps.carousel.spacing ? { [cssVar("carousel_spacing")]: spacing !== 0 ? spacing : "0px" } : null
  } }, items);
};
var CarouselModule = createModule("carousel", Carousel);

// node_modules/yet-another-react-lightbox/dist/core/modules/Core.js
var React13 = __toESM(require_react(), 1);
var Core = ({ children }) => React13.createElement(
  TimeoutsProvider,
  null,
  React13.createElement(EventsProvider, null, children)
);
var CoreModule = createModule("core", Core);

// node_modules/yet-another-react-lightbox/dist/core/modules/Navigation.js
var React14 = __toESM(require_react(), 1);
var NavigationButton = ({ publish, labels, label: label2, icon, renderIcon, action, disabled }) => React14.createElement(IconButton, { label: label(labels, label2), icon, renderIcon, className: cssClass(`navigation_${action}`), disabled, "aria-disabled": disabled, onClick: () => {
  publish(action);
} });
var Navigation = ({ slides, carousel: { finite }, labels, render: { buttonPrev, buttonNext, iconPrev, iconNext } }) => {
  const { currentIndex, subscribeSensors } = useController();
  const { publish } = useEvents();
  React14.useEffect(() => subscribeSensors("onKeyUp", (event) => {
    if (event.code === "ArrowLeft") {
      publish("prev");
    } else if (event.code === "ArrowRight") {
      publish("next");
    }
  }), [subscribeSensors, publish]);
  return React14.createElement(
    React14.Fragment,
    null,
    buttonPrev ? buttonPrev() : React14.createElement(NavigationButton, { label: "Previous Image", action: "prev", icon: PreviousIcon, renderIcon: iconPrev, disabled: finite && currentIndex === 0, labels, publish }),
    buttonNext ? buttonNext() : React14.createElement(NavigationButton, { label: "Next Image", action: "next", icon: NextIcon, renderIcon: iconNext, disabled: finite && currentIndex === slides.length - 1, labels, publish })
  );
};
var NavigationModule = createModule("navigation", Navigation);

// node_modules/yet-another-react-lightbox/dist/core/modules/NoScroll.js
var React15 = __toESM(require_react(), 1);
var noScroll = cssClass("no_scroll");
var padScrollbar = cssClass("pad_scrollbar");
var scrollbarPadding = cssVar("scrollbar_padding");
var NoScroll = ({ children }) => {
  React15.useEffect(() => {
    const scrollbarWidth = Math.round(window.innerWidth - document.documentElement.clientWidth);
    if (scrollbarWidth > 1) {
      document.body.style.setProperty(scrollbarPadding, `${scrollbarWidth}px`);
      document.body.classList.add(padScrollbar);
    }
    document.body.classList.add(noScroll);
    return () => {
      document.body.style.removeProperty(scrollbarPadding);
      document.body.classList.remove(noScroll, padScrollbar);
    };
  });
  return React15.createElement(React15.Fragment, null, children);
};
var NoScrollModule = createModule("no-scroll", NoScroll);

// node_modules/yet-another-react-lightbox/dist/core/modules/Portal.js
var React16 = __toESM(require_react(), 1);
var ReactDOM = __toESM(require_react_dom(), 1);
var Portal = ({ children, ...props }) => {
  const [mounted, setMounted] = React16.useState(false);
  const latestProps = useLatest(props);
  const { setTimeout } = useTimeouts();
  const { subscribe } = useEvents();
  const [portal] = React16.useState(() => {
    const div = document.createElement("div");
    div.className = cssClass("portal");
    const fadeAnimation = latestProps.current.animation.fade;
    if (fadeAnimation !== LightboxDefaultProps.animation.fade) {
      div.style.setProperty(cssVar("fade_animation_duration"), `${Math.round(fadeAnimation)}ms`);
    }
    return div;
  });
  React16.useEffect(() => subscribe("close", () => {
    var _a, _b;
    (_b = (_a = latestProps.current.on).exiting) === null || _b === void 0 ? void 0 : _b.call(_a);
    portal.classList.remove(cssClass("portal_open"));
    setTimeout(() => {
      var _a2, _b2;
      (_b2 = (_a2 = latestProps.current.on).exited) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
      latestProps.current.close();
    }, latestProps.current.animation.fade);
  }), [portal, setTimeout, subscribe, latestProps]);
  React16.useEffect(() => {
    var _a, _b;
    document.body.appendChild(portal);
    setMounted(true);
    (_b = (_a = latestProps.current.on).entering) === null || _b === void 0 ? void 0 : _b.call(_a);
    setTimeout(() => {
      portal.classList.add(cssClass("portal_open"));
    }, 0);
    setTimeout(() => {
      var _a2, _b2;
      (_b2 = (_a2 = latestProps.current.on).entered) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
    }, latestProps.current.animation.fade);
    return () => {
      document.body.removeChild(portal);
      setMounted(false);
    };
  }, [portal, setTimeout, latestProps]);
  return ReactDOM.createPortal(mounted ? children : null, portal);
};
var PortalModule = createModule("portal", Portal);

// node_modules/yet-another-react-lightbox/dist/core/modules/Toolbar.js
var React17 = __toESM(require_react(), 1);
var Toolbar = ({ toolbar: { buttons }, labels, render: { buttonClose, iconClose } }) => {
  const { publish } = useEvents();
  const renderCloseButton = () => buttonClose ? buttonClose() : React17.createElement(IconButton, { key: "close", label: label(labels, "Close"), icon: CloseIcon, renderIcon: iconClose, onClick: () => publish("close") });
  return React17.createElement("div", { className: cssClass("toolbar") }, buttons === null || buttons === void 0 ? void 0 : buttons.map((button) => button === "close" ? renderCloseButton() : button));
};
var ToolbarModule = createModule("toolbar", Toolbar);

// node_modules/yet-another-react-lightbox/dist/Lightbox.js
var renderNode = (node, props) => {
  var _a;
  return React18.createElement(node.module.component, { key: node.module.name, ...props }, (_a = node.children) === null || _a === void 0 ? void 0 : _a.map((child) => renderNode(child, props)));
};
var LightboxComponent = (props) => {
  const { plugins } = props;
  const { config, augmentation } = withPlugins([
    createNode(PortalModule, [
      createNode(NoScrollModule, [
        createNode(ControllerModule, [
          createNode(CarouselModule),
          createNode(ToolbarModule),
          createNode(NavigationModule)
        ])
      ])
    ])
  ], plugins);
  const augmentedProps = augmentation(props);
  if (!augmentedProps.open)
    return null;
  return React18.createElement(React18.Fragment, null, renderNode(createNode(CoreModule, config), augmentedProps));
};
LightboxComponent.propTypes = LightboxPropTypes;
LightboxComponent.defaultProps = LightboxDefaultProps;
var Lightbox = (props) => {
  const { carousel, animation, ...restProps } = props;
  const { carousel: defaultCarousel, animation: defaultAnimation, ...restDefaultProps } = LightboxDefaultProps;
  return React18.createElement(LightboxComponent, { carousel: { ...defaultCarousel, ...carousel }, animation: { ...defaultAnimation, ...animation }, ...restDefaultProps, ...restProps });
};
export {
  Lightbox,
  LightboxDefaultProps,
  LightboxPropTypes,
  SlideTypesPropTypes
};
//# sourceMappingURL=yet-another-react-lightbox.js.map
